<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black">

    <!-- Full-screen hardware video surface. -->
    <com.alexvas.rtsp.widget.RtspSurfaceView
        android:id="@+id/rtsp_view"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Black transition cover: masks the cross-feed/mosaic during a switch.
         Sits above the surface but below the controls. Lifted on first frame. -->
    <View
        android:id="@+id/transition_cover"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@android:color/black"
        android:visibility="visible"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Recording timer indicator (top-right). Hidden unless recording. -->
    <TextView
        android:id="@+id/txt_rec"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:layout_marginEnd="12dp"
        android:background="@drawable/txt_rec_bg"
        android:paddingHorizontal="10dp"
        android:paddingVertical="4dp"
        android:fontFamily="monospace"
        android:textColor="#FFFFFFFF"
        android:textSize="11sp"
        android:textStyle="bold"
        android:visibility="gone"
        tools:visibility="visible"
        tools:text="● REC  00:12"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Record toggle (top-left). Text flips between ● REC and ■ STOP; tint flips
         red (recording) / grey (idle) in onRecordingStateChanged. -->
    <Button
        android:id="@+id/btn_record"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="12dp"
        android:minWidth="0dp"
        android:minHeight="0dp"
        android:paddingHorizontal="12dp"
        android:paddingVertical="6dp"
        android:text="● Rec Off"
        android:textColor="#FFFFFFFF"
        android:textStyle="bold"
        android:textSize="12sp"
        android:background="@drawable/bg_record_pill"
        android:backgroundTint="#9E9E9E"
        android:stateListAnimator="@null"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Push-to-talk (top-left, under REC). Always visible. Resting = listening
         (incoming audio plays, mic off). Press/hold = talking (mic on, incoming
         audio muted). Tint flips green (talking) / grey (listening) in code.
         UDP to 192.168.144.10:5555. -->
    <Button
        android:id="@+id/btn_talk"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginTop="6dp"
        android:minWidth="0dp"
        android:minHeight="0dp"
        android:paddingHorizontal="12dp"
        android:paddingVertical="6dp"
        android:text="🔈 Listening"
        android:textColor="#FFFFFFFF"
        android:textStyle="bold"
        android:textSize="12sp"
        android:background="@drawable/bg_record_pill"
        android:backgroundTint="#9E9E9E"
        android:stateListAnimator="@null"
        app:layout_constraintTop_toBottomOf="@id/btn_record"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Left-side digital-zoom column, under the REC button (MAIN only).
         Scales the video surface from its center (1x/2x/3x/4x). -->
    <LinearLayout
        android:id="@+id/zoom_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginStart="12dp"
        android:layout_marginTop="16dp"
        android:visibility="gone"
        tools:visibility="visible"
        app:layout_constraintTop_toBottomOf="@id/btn_talk"
        app:layout_constraintStart_toStartOf="parent">

        <Button android:id="@+id/btn_zoom_1" style="@style/ZoomButton" android:text="1x" />
        <Button android:id="@+id/btn_zoom_2" style="@style/ZoomButton" android:text="2x" />
        <Button android:id="@+id/btn_zoom_3" style="@style/ZoomButton" android:text="3x" />
        <Button android:id="@+id/btn_zoom_4" style="@style/ZoomButton" android:text="4x" />
    </LinearLayout>

    <!-- Right-side SIYI gimbal pan/tilt D-pad (MAIN only). Hold an arrow to move,
         release to stop; center key recenters. Commands go to the A8 mini at
         192.168.144.25:37260. -->
    <LinearLayout
        android:id="@+id/gimbal_panel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginEnd="12dp"
        android:visibility="gone"
        tools:visibility="visible"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/nav_bar"
        app:layout_constraintEnd_toEndOf="parent">

        <Button android:id="@+id/btn_tilt_up" style="@style/GimbalButton" android:text="▲" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center">
            <Button android:id="@+id/btn_pan_left"      style="@style/GimbalButton" android:text="◀" />
            <Button android:id="@+id/btn_gimbal_center" style="@style/GimbalButton" android:text="⌂" />
            <Button android:id="@+id/btn_pan_right"     style="@style/GimbalButton" android:text="▶" />
        </LinearLayout>

        <Button android:id="@+id/btn_tilt_down" style="@style/GimbalButton" android:text="▼" />
    </LinearLayout>

    <!-- Bottom navigation overlay bar: 5 equal-weight feed buttons. -->
    <LinearLayout
        android:id="@+id/nav_bar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="6dp"
        android:background="#99000000"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <Button android:id="@+id/btn_front" style="@style/CamButton" android:text="FRONT" />
        <Button android:id="@+id/btn_back"  style="@style/CamButton" android:text="BACK" />
        <Button android:id="@+id/btn_wrist" style="@style/CamButton" android:text="WRIST" />
        <Button android:id="@+id/btn_grip"  style="@style/CamButton" android:text="GRIP" />
        <Button android:id="@+id/btn_main"  style="@style/CamButton" android:text="MAIN" />
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
