sudo ip link set can0 down && sudo ip link set can1 down && \
sudo ip link set can0 up type can bitrate 1000000 restart-ms 100 sample-point 0.875 triple-sampling on one-shot off listen-only off loopback off && \
sudo ip link set dev can0 txqueuelen 100 && \
sudo ip link set can1 up type can bitrate 1000000 restart-ms 100 sample-point 0.875 triple-sampling on one-shot off listen-only off loopback off && \
sudo ip link set dev can1 txqueuelen 100 && \
ip -details -statistics link show can0 && echo "------------------------" && ip -details -statistics link show can1
